package org.example.inteview;

import org.w3c.dom.Node;

import java.util.LinkedList;
/*
public class Merge_two_sorted_linklist {
    public LinkedList merge(){
        Node pointer1= linklist1;
        Node pointer2=linklist2;
        while(pointer1!=null && pointer2!=null){
            if (pointer1.value<=pointer2.value){
                newlinklist.next=pointer1.value;
                pointer1=pointer1.next;
            }
            else{
                newlinklist.next=pointer2.value;
                pointer2=pointer2.next;
            }
            newlinklist=newlinklist.next;

        }
        return newlinklist;

    }
}
*/

